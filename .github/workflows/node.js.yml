name: First workflow

on:  
  push:
    branches:
      -main


jobs:
  random-jobs:
    runs-on: ubuntu-latest

    steps:
      - name: Print a welcome message
        run: echo "Welcome to my place"
    
  
